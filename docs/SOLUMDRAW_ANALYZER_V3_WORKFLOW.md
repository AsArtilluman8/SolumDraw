# SolumDraw Analyzer v3 Workflow

SolumDraw is not only an image classifier. The final goal is:

```text
image / tags / prompt
-> genre, objects, style, layers, important regions
-> human drawing plan
-> stroke/fill/layer route
-> quality drawing, not printer-like output
